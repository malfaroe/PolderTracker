package com.mae.poldertracker.ui.theme

import androidx.compose.ui.graphics.Color

// Turquoise (primary)
val Turquoise80 = Color(0xFF80DEEA)    // Cyan 200 – dark mode primary
val Turquoise60 = Color(0xFF26C6DA)    // Cyan 400
val Turquoise40 = Color(0xFF00BCD4)    // Cyan 500 – light mode primary
val TurquoiseContainer = Color(0xFFB2EBF2) // Cyan 100

// Deep Blue (from reference image, ~#1212CC)
val DeepBlue = Color(0xFF1212CC)       // Royal blue – light mode secondary
val ElectricBlue = Color(0xFF6B7AFF)   // Lighter electric blue – dark mode secondary
val NavyBlue = Color(0xFF080B2E)       // Very dark blue – dark mode background
val NavySurface = Color(0xFF0F1450)    // Dark blue surface

// Neutrals
val Neutral90 = Color(0xFFF0F8FF)      // Alice blue tint
val Neutral20 = Color(0xFF0D1B2E)      // Deep blue-black

// Dark mode
val DarkSurface = Color(0xFF0F1450)
val DarkBackground = Color(0xFF080B2E)
